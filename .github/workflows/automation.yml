name: automationTest

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        next-version: [13.x]

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: install every dependency of the system
        run: npm ci

      - name: cd frontend
        run: cd frontend
      
      - name: ls 
        run: ls 
          
        
